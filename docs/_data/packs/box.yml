# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:08Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-box-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/box/db/hth-box-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk downloads
            SELECT user_email, COUNT(*) as download_count
            FROM box_events
            WHERE event_type = 'DOWNLOAD'
            AND created_at > NOW() - INTERVAL '1 hour'
            GROUP BY user_email
            HAVING COUNT(*) > 50;

