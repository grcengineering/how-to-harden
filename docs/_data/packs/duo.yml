# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-08-09T01:35:30Z

"1.1":
  sdk:
    lang: "python"
    filename: "hth-duo-1.01-audit-admin-roles.py"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/duo/sdk/hth-duo-1.01-audit-admin-roles.py"
    excerpts:
      sdk-audit-admin-roles:
        content: |2
          admin_api = duo_client.Admin(
              ikey=os.environ["DUO_IKEY"],
              skey=os.environ["DUO_SKEY"],
              host=os.environ["DUO_API_HOST"],  # e.g. api-XXXXXXXX.duosecurity.com
          )
          
          owners = []
          total = 0
          for admin in admin_api.get_admins_iterator():
              total += 1
              role = admin.get("role") or ""
              print(
                  f"{admin.get('name')}\t{admin.get('email')}\t"
                  f"role={role}\tstatus={admin.get('status')}"
              )
              if role.lower() == "owner":
                  owners.append(admin.get("email"))
          
          print(f"\n{total} administrator(s) total; {len(owners)} hold the Owner role:")
          for email in owners:
              print(f"  {email}")
          if len(owners) > 2:
              print("WARN: limit Owner to 1-2 accounts — only Owners can manage other")
              print("      admins and the Admin API / Account API applications (control 1.1)")

"2.2":
  sdk:
    lang: "python"
    filename: "hth-duo-2.02-audit-bypass-users.py"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/duo/sdk/hth-duo-2.02-audit-bypass-users.py"
    excerpts:
      sdk-audit-bypass-users:
        content: |2
          admin_api = duo_client.Admin(
              ikey=os.environ["DUO_IKEY"],
              skey=os.environ["DUO_SKEY"],
              host=os.environ["DUO_API_HOST"],
          )
          
          bypass_users = [
              user for user in admin_api.get_users_iterator()
              if user.get("status") == "bypass"
          ]
          
          if not bypass_users:
              print("PASS: no users in bypass status")
          else:
              print(f"WARN: {len(bypass_users)} user(s) bypass MFA entirely:")
              for user in bypass_users:
                  last_login = user.get("last_login")  # Unix timestamp or None
                  last_seen = (
                      time.strftime("%Y-%m-%d", time.gmtime(last_login))
                      if last_login else "never"
                  )
                  print(f"  {user.get('username')}\tlast_login={last_seen}")
              print("Bypass is for temporary troubleshooting only — set each user back")
              print("to Active or document an expiring exception (control 2.2).")

"3.1":
  sdk:
    lang: "python"
    filename: "hth-duo-3.01-report-inactive-users.py"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/duo/sdk/hth-duo-3.01-report-inactive-users.py"
    excerpts:
      sdk-report-inactive-users:
        content: |2
          admin_api = duo_client.Admin(
              ikey=os.environ["DUO_IKEY"],
              skey=os.environ["DUO_SKEY"],
              host=os.environ["DUO_API_HOST"],
          )
          
          cutoff = time.time() - STALE_DAYS * 86400
          never_enrolled = []
          stale = []
          
          for user in admin_api.get_users_iterator():
              if not user.get("is_enrolled"):
                  never_enrolled.append(user.get("username"))
                  continue
              last_login = user.get("last_login")  # Unix timestamp, or None if never
              if last_login is None or last_login < cutoff:
                  stale.append((user.get("username"), last_login))
          
          print(f"{len(never_enrolled)} user(s) provisioned but never enrolled — "
                "verify employment, then resend enrollment or delete:")
          for username in never_enrolled:
              print(f"  {username}")
          
          print(f"\n{len(stale)} enrolled user(s) with no login in {STALE_DAYS} days — "
                "disable until re-verified:")
          for username, last_login in stale:
              last_seen = (
                  time.strftime("%Y-%m-%d", time.gmtime(last_login))
                  if last_login else "never"
              )
              print(f"  {username}\tlast_login={last_seen}")

"6.1":
  sdk:
    lang: "python"
    filename: "hth-duo-6.01-export-authentication-logs.py"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/duo/sdk/hth-duo-6.01-export-authentication-logs.py"
    excerpts:
      sdk-export-authentication-logs:
        content: |2
          admin_api = duo_client.Admin(
              ikey=os.environ["DUO_IKEY"],
              skey=os.environ["DUO_SKEY"],
              host=os.environ["DUO_API_HOST"],
          )
          
          # Window defaults to the last 24 hours; both bounds are Unix ms timestamps.
          now_s = int(time.time())
          mintime = int(os.environ.get("DUO_LOG_MINTIME_MS", (now_s - 86400) * 1000))
          maxtime = int(os.environ.get("DUO_LOG_MAXTIME_MS", now_s * 1000))
          
          next_offset = None
          exported = 0
          while True:
              response = admin_api.get_authentication_log(
                  api_version=2,
                  mintime=mintime,
                  maxtime=maxtime,
                  next_offset=next_offset,
              )
              for event in response["authlogs"]:
                  sys.stdout.write(json.dumps(event) + "\n")
                  exported += 1
              next_offset = (response.get("metadata") or {}).get("next_offset")
              if not next_offset:
                  break
          
          print(f"exported {exported} authentication event(s)", file=sys.stderr)

