# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:37Z

"2.1":
  cli:
    lang: "text"
    filename: "hth-marketo-2.01-api-security-checklist.txt"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/marketo/cli/hth-marketo-2.01-api-security-checklist.txt"
    excerpts:
      cli-api-security-checklist:
        content: |
            API Security Checklist:
            ├── Create dedicated API users per integration
            ├── Use API-only users (no UI access)
            ├── Rotate Client Secret annually
            ├── Monitor API usage quotas
            └── Document all integrations

"4.1":
  db:
    lang: "sql"
    filename: "hth-marketo-4.01-detection-queries.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/marketo/db/hth-marketo-4.01-detection-queries.sql"
    excerpts:
      db-detect-bulk-exports:
        content: |
            -- Detect bulk lead exports
            SELECT user_email, export_type, lead_count
            FROM marketo_audit_log
            WHERE action = 'EXPORT_LEADS'
            AND lead_count > 10000
            AND timestamp > NOW() - INTERVAL '24 hours';
          
            -- Detect API abuse
            SELECT service_name, endpoint, COUNT(*) as calls
            FROM api_usage_log
            WHERE timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY service_name, endpoint
            HAVING COUNT(*) > 1000;

"4.2":
  db:
    lang: "sql"
    filename: "hth-marketo-4.02-integration-monitoring.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/marketo/db/hth-marketo-4.02-integration-monitoring.sql"
    excerpts:
      db-detect-new-services:
        content: |
            -- Detect new LaunchPoint services
            SELECT service_name, created_by, created_date
            FROM launchpoint_services
            WHERE created_date > NOW() - INTERVAL '7 days';
          
            -- Detect email template changes
            SELECT asset_name, modified_by, modification_type
            FROM audit_trail
            WHERE asset_type = 'EMAIL'
            AND modification_type IN ('APPROVE', 'UNAPPROVE')
            AND timestamp > NOW() - INTERVAL '24 hours';

