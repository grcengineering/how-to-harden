# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:52:00Z

"2.2":
  cli:
    lang: "text"
    filename: "hth-tableau-2.02-user-filters.txt"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/tableau/cli/hth-tableau-2.02-user-filters.txt"
    excerpts:
      cli-user-filter-calc:
        content: |
            # User filter calculation
            [Region] = USERNAME()
          
            # Or use groups
            ISMEMBEROF('Finance')

"4.1":
  db:
    lang: "sql"
    filename: "hth-tableau-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/tableau/db/hth-tableau-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-bulk-downloads:
        content: |
            -- Detect bulk data downloads
            SELECT user_name, workbook_name, download_count
            FROM admin_insights
            WHERE action = 'Download'
            AND timestamp > NOW() - INTERVAL '24 hours'
            GROUP BY user_name, workbook_name
            HAVING download_count > 10;
      db-detect-unusual-access:
        content: |
            -- Detect unusual access patterns
            SELECT user_name, site_role, view_count
            FROM traffic_to_views
            WHERE timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY user_name, site_role
            HAVING view_count > 100;

