# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:07Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-bamboohr-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/bamboohr/db/hth-bamboohr-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk data exports
            SELECT user_email, report_name, record_count
            FROM bamboo_activity
            WHERE action = 'report_export'
            AND record_count > 100
            AND timestamp > NOW() - INTERVAL '24 hours';
          
            -- Detect API abuse
            SELECT api_key, endpoint, COUNT(*) as calls
            FROM api_log
            WHERE timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY api_key, endpoint
            HAVING COUNT(*) > 500;

