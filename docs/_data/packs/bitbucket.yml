# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-09-12T15:57:26Z

"4.2":
  config:
    lang: "yaml"
    filename: "hth-bitbucket-4.02-deployment-pipeline.yml"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/bitbucket/config/hth-bitbucket-4.02-deployment-pipeline.yml"
    excerpts:
      cli-deployment-pipeline:
        content: |2
          pipelines:
            branches:
              main:
                - step:
                    name: Build
                    script:
                      # Use `ci`, never the resolving installer: `ci` honours the lockfile
                      # exactly, while the resolving form can silently pick up a newer
                      # transitive version at build time.
                      # --ignore-scripts blocks pre/post-install hooks, the execution path
                      # used by the chalk/debug, solana-web3.js and Shai-Hulud npm
                      # compromises. Drop it only for a dependency that genuinely needs a
                      # build step, and say which one in review.
                      - npm ci --ignore-scripts
                      - npm run build
                - step:
                    name: Deploy to Production
                    deployment: production
                    trigger: manual
                    script:
                      - ./deploy.sh

"4.3":
  config:
    lang: "yaml"
    filename: "hth-bitbucket-4.03-secret-scan-pipeline.yml"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/bitbucket/config/hth-bitbucket-4.03-secret-scan-pipeline.yml"
    excerpts:
      cli-secret-scan-pipeline:
        content: |2
          pipelines:
            default:
              - step:
                  name: Secret Scan
                  script:
                    - pip install trufflehog
                    - trufflehog --regex --entropy=False .

