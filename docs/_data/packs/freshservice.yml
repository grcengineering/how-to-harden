# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:21Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-freshservice-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/freshservice/db/hth-freshservice-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk asset exports
            SELECT agent_email, export_type, record_count
            FROM freshservice_audit
            WHERE action = 'export'
            AND module = 'asset'
            AND record_count > 100;
          
            -- Detect unusual API activity
            SELECT api_key, endpoint, COUNT(*) as calls
            FROM api_log
            WHERE timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY api_key, endpoint
            HAVING COUNT(*) > 500;

