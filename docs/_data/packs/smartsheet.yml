# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:56Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-smartsheet-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/smartsheet/db/hth-smartsheet-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk sharing changes
            SELECT user_email, sheet_name, action
            FROM smartsheet_activity
            WHERE action LIKE '%share%'
            AND timestamp > NOW() - INTERVAL '24 hours';
          
            -- Detect unusual export activity
            SELECT user_email, export_count
            FROM smartsheet_activity
            WHERE action = 'export'
            AND timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY user_email
            HAVING export_count > 20;

