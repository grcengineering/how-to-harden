# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:19Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-dropbox-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/dropbox/db/hth-dropbox-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk downloads
            SELECT user_email, COUNT(*) as download_count
            FROM dropbox_activity
            WHERE action = 'file_download'
            AND timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY user_email
            HAVING COUNT(*) > 100;
          
            -- Detect external sharing
            SELECT user_email, shared_with, file_path
            FROM dropbox_activity
            WHERE action = 'share_link_create'
            AND is_external = true
            AND timestamp > NOW() - INTERVAL '24 hours';

