# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:52Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-rippling-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/rippling/db/hth-rippling-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk data access
            SELECT admin_email, action, record_count
            FROM rippling_audit_log
            WHERE action LIKE '%export%'
            AND record_count > 50
            AND timestamp > NOW() - INTERVAL '24 hours';
          
            -- Detect app provisioning changes
            SELECT admin_email, app_name, action
            FROM rippling_audit_log
            WHERE action IN ('app.add_user', 'app.remove_user')
            AND timestamp > NOW() - INTERVAL '7 days';

