# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-08-09T01:38:50Z

"2.1":
  api:
    lang: "python"
    filename: "hth-notion-2.01-audit-workspace-members.py"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/notion/api/hth-notion-2.01-audit-workspace-members.py"
    excerpts:
      api-audit-workspace-members:
        content: |2
          headers = {
              "Authorization": f"Bearer {NOTION_TOKEN}",
              "Notion-Version": "2026-03-11",
          }
          
          people, bots, flagged = [], [], []
          cursor = None
          while True:
              params = {"page_size": 100}
              if cursor:
                  params["start_cursor"] = cursor
              response = requests.get(
                  "https://api.notion.com/v1/users", headers=headers, params=params
              )
              response.raise_for_status()
              data = response.json()
          
              for user in data["results"]:
                  if user["type"] == "bot":
                      bots.append(user.get("name"))
                  else:
                      email = (user.get("person") or {}).get("email") or ""
                      people.append(email)
                      domain = email.split("@")[-1].lower() if "@" in email else ""
                      if domain not in ALLOWED_DOMAINS:
                          flagged.append(f"{user.get('name')} <{email or 'email not visible'}>")
          
              if not data.get("has_more"):
                  break
              cursor = data["next_cursor"]
          
          print(f"{len(people)} person user(s), {len(bots)} bot/integration user(s)")
          for name in bots:
              print(f"  bot: {name}")
          if flagged:
              print(f"\nWARN: {len(flagged)} member(s) outside allowed email domains:")
              for entry in flagged:
                  print(f"  {entry}")
              print("Remove unauthorized members and restrict allowed email domains (control 2.1).")

"3.1":
  api:
    lang: "python"
    filename: "hth-notion-3.01-audit-public-pages.py"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/notion/api/hth-notion-3.01-audit-public-pages.py"
    excerpts:
      api-audit-public-pages:
        content: |2
          headers = {
              "Authorization": f"Bearer {NOTION_TOKEN}",
              "Notion-Version": "2026-03-11",
          }
          
          public_pages = []
          scanned = 0
          payload = {
              "filter": {"property": "object", "value": "page"},
              "page_size": 100,
          }
          while True:
              response = requests.post(
                  "https://api.notion.com/v1/search", headers=headers, json=payload
              )
              response.raise_for_status()
              data = response.json()
          
              for page in data["results"]:
                  scanned += 1
                  if page.get("public_url"):
                      public_pages.append(page)
          
              if not data.get("has_more"):
                  break
              payload["start_cursor"] = data["next_cursor"]
          
          print(f"scanned {scanned} page(s) shared with this integration")
          if not public_pages:
              print("PASS: no pages published to the web")
          else:
              print(f"WARN: {len(public_pages)} page(s) reachable by anyone on the internet:")
              for page in public_pages:
                  print(
                      f"  {page['id']}\tlast_edited={page.get('last_edited_time')}\t"
                      f"{page['public_url']}"
                  )
              print("Unpublish any page not deliberately public (control 3.1).")

