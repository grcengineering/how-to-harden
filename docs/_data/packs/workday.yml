# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:52:04Z

"1.2":
  cli:
    lang: "text"
    filename: "hth-workday-1.02-security-group-structure.txt"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/workday/cli/hth-workday-1.02-security-group-structure.txt"
    excerpts:
      cli-security-group-structure:
        content: |
            Security Groups:
            ├── HR Business Partner
            │   └── View employee data for assigned organizations
            ├── Compensation Administrator
            │   └── Manage compensation (restricted fields)
            ├── Benefits Administrator
            │   └── Manage benefits enrollment
            ├── Payroll Administrator
            │   └── Process payroll (segregation of duties)
            ├── Integration System User
            │   └── API access for specific integrations
            └── Security Administrator
              └── Manage security configuration

"2.1":
  cli:
    lang: "text"
    filename: "hth-workday-2.01-isu-architecture.txt"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/workday/cli/hth-workday-2.01-isu-architecture.txt"
    excerpts:
      cli-isu-architecture:
        content: |
            ISU Architecture:
            ├── ISU-ADP-Payroll (payroll data only)
            ├── ISU-Benefits-Carrier (benefits data only)
            ├── ISU-HRIS-Reporting (read-only reporting)
            ├── ISU-Recruiting-ATS (applicant data only)
            └── ISU-IT-Provisioning (worker provisioning)

"3.1":
  cli:
    lang: "text"
    filename: "hth-workday-3.01-sensitive-fields.txt"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/workday/cli/hth-workday-3.01-sensitive-fields.txt"
    excerpts:
      cli-sensitive-fields:
        content: |
            High Sensitivity:
            - Social Security Number
            - Bank Account Numbers
            - Salary/Compensation
            - Performance Reviews
            - Medical Information
          
            Medium Sensitivity:
            - Home Address
            - Personal Email
            - Emergency Contacts
            - Birthdates

"5.1":
  db:
    lang: "sql"
    filename: "hth-workday-5.01-detection-queries.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/workday/db/hth-workday-5.01-detection-queries.sql"
    excerpts:
      db-detect-bulk-access:
        content: |
            -- Detect bulk data access
            SELECT user_id, COUNT(*) as record_count
            FROM workday_audit_log
            WHERE action = 'View'
            AND object_type IN ('Worker', 'Compensation')
            AND timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY user_id
            HAVING COUNT(*) > 100;
          
            -- Detect ISU anomalies
            SELECT isu_name, api_endpoint, COUNT(*) as call_count
            FROM api_access_log
            WHERE timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY isu_name, api_endpoint
            HAVING COUNT(*) > 1000;
          
            -- Detect sensitive field access
            SELECT user_id, field_name, worker_id
            FROM field_access_log
            WHERE field_name IN ('SSN', 'Bank_Account', 'Salary')
            AND timestamp > NOW() - INTERVAL '24 hours';

