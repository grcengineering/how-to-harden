# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:40Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-miro-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/miro/db/hth-miro-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect board sharing changes
            SELECT user_email, board_name, share_type
            FROM miro_audit_log
            WHERE action = 'board_share_change'
            AND share_type = 'public'
            AND timestamp > NOW() - INTERVAL '7 days';
          
            -- Detect bulk exports
            SELECT user_email, export_count
            FROM miro_audit_log
            WHERE action = 'board_export'
            AND timestamp > NOW() - INTERVAL '24 hours'
            GROUP BY user_email
            HAVING export_count > 10;

