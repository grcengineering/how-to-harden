# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-08-09T01:41:28Z

"2.1":
  api:
    lang: "bash"
    filename: "hth-zoom-2.01-meeting-security-settings-audit.sh"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/zoom/api/hth-zoom-2.01-meeting-security-settings-audit.sh"
    excerpts:
      api-audit-meeting-security:
        content: |2
          
          # --- Mint a Server-to-Server OAuth access token (valid one hour) ---
          ZOOM_TOKEN=$(curl -s -X POST "https://zoom.us/oauth/token" \
            -H "Authorization: Basic $(printf '%s:%s' "${ZOOM_CLIENT_ID}" "${ZOOM_CLIENT_SECRET}" | base64 | tr -d '\n')" \
            -d "grant_type=account_credentials" \
            -d "account_id=${ZOOM_ACCOUNT_ID}" | jq -r '.access_token')
          
          # --- Audit: passcode, waiting-room, and authentication posture ---
          # schedule_meeting / in_meeting / meeting_security are the documented
          # account-settings groups that carry this control's toggles.
          echo "=== Account settings: schedule_meeting, in_meeting, meeting_security ==="
          curl -s -H "Authorization: Bearer ${ZOOM_TOKEN}" \
            "https://api.zoom.us/v2/accounts/me/settings" | \
            jq '{schedule_meeting, in_meeting, meeting_security}'
          
          # --- Audit: are those settings LOCKED at the account level? ---
          # An enabled-but-unlocked setting can be weakened by any group or host.
          echo ""
          echo "=== Account-level locked settings ==="
          curl -s -H "Authorization: Bearer ${ZOOM_TOKEN}" \
            "https://api.zoom.us/v2/accounts/me/lock_settings" | jq '.'

"4.2":
  api:
    lang: "bash"
    filename: "hth-zoom-4.02-recording-security-settings-audit.sh"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/zoom/api/hth-zoom-4.02-recording-security-settings-audit.sh"
    excerpts:
      api-audit-recording-security:
        content: |2
          
          # --- Mint a Server-to-Server OAuth access token (valid one hour) ---
          ZOOM_TOKEN=$(curl -s -X POST "https://zoom.us/oauth/token" \
            -H "Authorization: Basic $(printf '%s:%s' "${ZOOM_CLIENT_ID}" "${ZOOM_CLIENT_SECRET}" | base64 | tr -d '\n')" \
            -d "grant_type=account_credentials" \
            -d "account_id=${ZOOM_ACCOUNT_ID}" | jq -r '.access_token')
          
          # --- Audit: the account's recording settings group ---
          # "recording" is the documented account-settings group carrying cloud
          # recording, consent, and access-protection toggles. Review every value
          # against the guide's required posture.
          echo "=== Account settings: recording ==="
          curl -s -H "Authorization: Bearer ${ZOOM_TOKEN}" \
            "https://api.zoom.us/v2/accounts/me/settings" | \
            jq '.recording'
          
          # --- Audit: is the recording configuration locked account-wide? ---
          echo ""
          echo "=== Account-level locks covering recording ==="
          curl -s -H "Authorization: Bearer ${ZOOM_TOKEN}" \
            "https://api.zoom.us/v2/accounts/me/lock_settings" | \
            jq 'if has("recording") then {recording} else . end'

