# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:52:05Z

"5.1":
  db:
    lang: "sql"
    filename: "hth-zoom-5.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/zoom/db/hth-zoom-5.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect unusual meeting creation
            SELECT user_id, COUNT(*) as meeting_count
            FROM zoom_meetings
            WHERE created_at > NOW() - INTERVAL '1 hour'
            GROUP BY user_id
            HAVING COUNT(*) > 20;
          
            -- Detect recording access anomalies
            SELECT user_id, recording_id, access_time
            FROM recording_access_log
            WHERE user_id NOT IN (SELECT host_id FROM meetings WHERE id = recording_id);

