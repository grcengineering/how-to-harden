# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:36Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-klaviyo-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/klaviyo/db/hth-klaviyo-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk profile exports
            SELECT user_email, export_type, profile_count
            FROM klaviyo_activity
            WHERE action = 'export'
            AND profile_count > 10000
            AND timestamp > NOW() - INTERVAL '24 hours';
          
            -- Detect API abuse
            SELECT api_key_prefix, endpoint, COUNT(*) as calls
            FROM api_log
            WHERE timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY api_key_prefix, endpoint
            HAVING COUNT(*) > 5000;

