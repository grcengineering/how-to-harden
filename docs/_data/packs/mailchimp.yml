# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:51:37Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-mailchimp-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/mailchimp/db/hth-mailchimp-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk exports
            SELECT user_email, export_type, record_count
            FROM mailchimp_activity
            WHERE action = 'export'
            AND record_count > 1000
            AND timestamp > NOW() - INTERVAL '24 hours';
          
            -- Detect suspicious campaign creation
            SELECT user_email, campaign_name, audience_size
            FROM campaign_log
            WHERE created_at > NOW() - INTERVAL '24 hours'
            AND audience_size > 10000;

