# AUTO-GENERATED by scripts/sync-packs-to-data.sh — do not edit manually
# Generated: 2026-02-19T16:52:04Z

"4.1":
  db:
    lang: "sql"
    filename: "hth-zendesk-4.01-detect-anomalies.sql"
    source_url: "https://github.com/grcengineering/how-to-harden/blob/main/packs/zendesk/db/hth-zendesk-4.01-detect-anomalies.sql"
    excerpts:
      db-detect-anomalies:
        content: |
            -- Detect bulk ticket exports
            SELECT user_email, action, COUNT(*) as exports
            FROM zendesk_audit_log
            WHERE action = 'ticket_export'
            AND timestamp > NOW() - INTERVAL '24 hours'
            GROUP BY user_email, action
            HAVING COUNT(*) > 10;
          
            -- Detect API abuse
            SELECT api_token, endpoint, COUNT(*) as requests
            FROM api_log
            WHERE timestamp > NOW() - INTERVAL '1 hour'
            GROUP BY api_token, endpoint
            HAVING COUNT(*) > 1000;

