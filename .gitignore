node_modules/
MEMORY/
.zerg/
.DS_Store
.gsd/
.claude/settings.json
.claude/settings.local.json
.claude/projects/
.claude/metadata-updater.sh

# Jekyll / Bundler local build artifacts (regenerated by `bundle install` + `jekyll build`)
docs/_site/
docs/vendor/
docs/.bundle/

# validate-hth-guide run artifacts — findings ledger, evidence, scratch.
# NEVER commit: a live-tenant validation run captures tokens, cookies, org
# identifiers, and member emails that must not reach a public repo.
.hth-validation/
